[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "anvil-harness"
version = "0.1.0"
description = "Direct local-model coding capability benchmark harness"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["anvil*"]

